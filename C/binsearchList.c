#include <stdio.h>
#include <stdlib.h>

struct Node {
	int data;
	struct Node* next;
}

struct LinkedList {
	Node node;	
}

struct Node* binsearchList(Node *head, int find) {
	struct Node* start = head;
	struct Node* end = NULL;
	
}

void main(){

}
