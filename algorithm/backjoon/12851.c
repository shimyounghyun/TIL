#include <stdio.h>
#include <stdlib.h>
const int MAX=1e6;
int n,m;
int map[MAX+1];

int main()
{
	int queue[MAX+1];
	int front=0;
	int rear=0;
	int ans=0,cnt=0;
	scanf("%d %d", &n, &m);
	queue[rear++]=
	while (!cnt){
		int d = 
	}
}
